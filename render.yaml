services:
  - type: web
    name: bya-market-desk
    env: node
    plan: free
    buildCommand: ""
    startCommand: "npm start"
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
